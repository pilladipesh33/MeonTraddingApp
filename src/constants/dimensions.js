import { Dimensions } from 'react-native'

export let SCREEN_HEIGHT = Dimensions.get('screen').height;
export let SCREEN_WIDTH = Dimensions.get('screen').width;
export let WINDOW_HEIGHT = Dimensions.get('window').height;